<h1>New Receipt	</h1>
	<%= %>
	<%= form_for [:inbound, @receipt]  do |form|	%>
		<%= form.hidden_field :warehouse_id, :value => @warehouse.id	%>
		<%= render :partial => 'receipt', :locals => {:form => form}	%>
		<%= submit_tag "Create Receipt"									%>
	<% end %>
<%= link_to "Back To Receipts", lookup_inbound_receipts_path	%>
