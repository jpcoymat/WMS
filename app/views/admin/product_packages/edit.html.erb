<h4>Edit Product Package</h4>
	<%= form_for [:admin, @product, @product_package] do |form| %>
		<%= form.hidden_field :id %>
		<%= render :partial => 'product_package', :locals => {:form => form}	%>
		<%= submit_tag "Update Product Package"%>
	<% end %>
